# 安全基线

- 密码使用成熟认证方案和强哈希；
- Session/Token 最小权限、短时效、可撤销；
- CSRF、XSS、SQL 注入、SSRF、路径遍历、文件上传、重放攻击均有测试；
- 连接器密钥加密存储，不进入日志和前端；
- 文件证据做类型、大小、病毒和访问权限检查；
- 速率限制按 IP、用户、租户和关键动作分层；
- 数据导出有审批、过期链接、水印和审计；
- AI 提示词不得包含不必要的敏感数据；
- 日志对手机号、身份证、密钥等做脱敏；
- 备份和恢复必须定期演练。
