# 第一阶段：可商用闭环施工计划（AI 主导）

- recorded_at: 2026-08-10 Asia/Shanghai
- executor: Cursor Agent
- claim_boundary: 第一阶段目标 = **公网可试用 + 主商业闭环可跑通 + 推广级视觉底线**。二次更新：九角色全包、像素级一线对标、自由拖拽 DAG。
- note: 仓库内**不得**写入密钥/密码。云账号与证书只放本机密钥库或你私发通道。

## 0. 科学排序（先闭环，再上云，再推广面）

| 序 | 闸门 | 谁做 | 完成标准 |
| -- | ---- | ---- | -------- |
| **P1-A** | 本地商用主闭环再验收 | AI | 消费者→员工→老板→平台 一条链可证据化；无假三方对接 |
| **P1-B** | 推广级视觉/IA 底线 | AI | 四端壳一致、关键态齐全、首屏可拿去讲；非像素级对标 |
| **P1-C** | 公网 HTTPS 受控试点 | AI 部署 + 你提供账号物料 | `https://` 四端可打开；API fail-closed 生产配置通过 |
| **P1-D** | 你签字 | 仅你 | `PRODUCT_OWNER_UI_ACCEPTANCE` + 试点清单 PASS |
| **P1-E** | 对外可说「受控商用试用」 | 你 | 仅在 P1-A…D 全绿后 |

并行规则：P1-A / P1-B 可与「物料齐备」并行；**无云物料则 P1-C 只能做剧本与脚本，不能真上线**。

## 1. AI 能闭环的（你不用盯）

1. 主链路缺口补齐（配置壳、同步收敛、会员发放/核销时间线等必要项）
2. 推广级视觉底线（`@oneday/ui` + 四端壳，禁止页面级乱补丁）
3. 部署剧本：Compose/CVM、反向代理、环境变量模板、健康检查、回滚说明
4. 自动测试 + `evidence/` + 提交推送

## 2. 必须你提供、且勿提交进 Git 的物料

当前仓库事实：`EXECUTOR_HANDOFF` 记载 **repo 内无腾讯云文档、无 .env**；旧授权 **G = 本阶段不做公网**。  
你说「以前给过」——若只在聊天里给过，**本仓库执行器读不到**。请按下方清单再发一次（私聊/本机文件均可，**不要贴进 Git**）。

### 清单（缺哪项回哪项）

| # | 物料 | 用途 |
| - | ---- | ---- |
| 1 | **明确授权：放开 G**（一句话即可：「授权公网 HTTPS / 腾讯云试点」） | 否则 Agent 按规则不得上云 |
| 2 | 腾讯云 **账号登录方式**（子用户/协作者，最小权限）或 API 密钥投放位置说明 | 建 CVM/CLB/CDN/证书 |
| 3 | **地域**（如 ap-guangzhou）与是否已有 CVM / 轻量 / 容器 | 选部署形态 |
| 4 | **域名**（API + 四端，或一个主域+子域方案） | `api.` / `c.` / `e.` / `m.` / `p.` 或路径分流 |
| 5 | 域名是否已在腾讯云 DNS；**DNS 修改权**是否在你 | 解析到试点 IP/CLB |
| 6 | **证书**：已签发到哪、是否可用腾讯云免费证书、还是你提供 PEM | HTTPS 终结 |
| 7 | 受控 **PostgreSQL**：云数据库连接串投放方式，或允许 Agent 在 CVM 上起 Docker Postgres（仅试点） | `DATABASE_URL` |
| 8 | 生产用 **`AUTH_TOKEN_SECRET`**（≥32 位随机，非开发默认）投放位置 | API 启动 |
| 9 | 试点 **CORS 精确源**列表（四端 https 源） | 生产 CORS fail-closed |
| 10 | 是否允许公网打开 **Consumer 试用租户**（slug + 是否用 fixture/seed） | 客户怎么进门 |
| 11 | 管理员手机号/邮箱（用于发试点账号，**不要用仓库种子密码上公网**） | 人号隔离 |

你回复时只需：「1 授权…；2–11 已放在 xxx / 如下…」。密钥本体优先本机路径或密码管理器，避免长期留在聊天。

## 3. 二次更新（明确不做进第一阶段）

- 完整九角色产品包
- 一线大厂像素级视觉对标
- 自由拖拽 DAG
- 多地域高可用、完整 WAF/观测大盘

## 4. 新窗口默认施工令（授权 G 放开且物料齐后）

1. 读本文件 + `DECISION_REQUIRED` + `git status`
2. 只做第一阶段 P1-A→C；不扩二次更新
3. 密钥不入库；缺物料写 `BLOCKED_REPORT` 并列缺失项
4. 连续施工至 90%–95% 换窗；你无需盯过程
